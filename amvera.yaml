meta:
  environment: node
  toolchain:
    name: node
    version: 20

build:
  artifactsPath: .
  run: |
    cd bot
    npm ci
    npm run build

run:
  persistenceMount: /data
  command: node bot/dist/index.js
  env:
    NODE_ENV: production